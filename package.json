{
  "name": "democi",
  "version": "1.0.0",
  "description": "Simple demo app for clone -> build -> push -> deploy pipeline",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
